FROM node:18-alpine
WORKDIR /app

# Copy root package files and init script
COPY package*.json ./
COPY scripts/ ./scripts/

# Install dependencies (pg and bcryptjs)
RUN npm install --omit=dev

# Run the database initialization script
CMD ["node", "scripts/init-db.js"]
